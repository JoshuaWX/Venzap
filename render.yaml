services:
  - type: web
    name: venzap-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

  - type: web
    name: venzap-celery
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python render_worker_entry.py
    autoDeploy: true
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

  - type: web
    name: venzap-bot
    env: python
    rootDir: bot
    buildCommand: pip install -r requirements.txt
    startCommand: python render_bot_entry.py
    autoDeploy: true
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
